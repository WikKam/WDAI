import { RoutescrollDirective } from './routescroll.directive';

describe('RoutescrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutescrollDirective();
    expect(directive).toBeTruthy();
  });
});
